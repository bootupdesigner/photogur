<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @picture.title %>
</p>

<p>
  <strong>Image</strong>
  </p>
  <figure>
  <%= image_tag @picture.url, class: "img-show", style: 'max-width: 100%; height: 20rem;' %>
  </figure>

<p>
  <strong>Description:</strong>
  <%= @picture.description %>
</p>


<% if logged_in? && (current_user.id == @picture.user_id) %>
<%= link_to 'Edit', edit_picture_path(@picture) %> |
<% end %>
<%= link_to 'Back', pictures_path %>
